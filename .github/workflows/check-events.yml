name: Check Om Swami Events

on:
  schedule:
    # Check every 5 minutes for new events
    # Events fill up fast (15-20 min), so frequent checks are crucial
    - cron: '*/5 * * * *'
  
  # Allows manual trigger from GitHub UI for testing
  workflow_dispatch:

jobs:
  check-events:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write  # Required to commit events_data.json
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'  # Cache pip dependencies for faster builds
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
    
    - name: Create config file from secrets
      run: |
        cat > config.json << EOF
        {
          "email": {
            "smtp_server": "${{ secrets.SMTP_SERVER }}",
            "smtp_port": ${{ secrets.SMTP_PORT }},
            "sender_email": "${{ secrets.SENDER_EMAIL }}",
            "sender_password": "${{ secrets.SENDER_PASSWORD }}",
            "recipient_emails": ${{ secrets.RECIPIENT_EMAILS }}
          },
          "check_interval_minutes": 5
        }
        EOF
    
    - name: Run event tracker
      run: |
        python event_tracker.py --once
    
    - name: Commit updated events data
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: "Update events data [skip ci]"
        file_pattern: events_data.json
        commit_user_name: github-actions[bot]
        commit_user_email: github-actions[bot]@users.noreply.github.com
    
    - name: Upload logs (if failed)
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: event-tracker-logs-${{ github.run_number }}
        path: event_tracker.log
        retention-days: 7  # Keep logs for 7 days
